# Prompt — second correction pass (paste into the Claude Design canvas)

Paste the whole thing. Seven items, none of them visual. The previous pass did the mechanical work —
the type ramp now floors at 16px, every artboard holds a 96/54 safe margin, the AA contrast failures
are gone and the text palette is down to eleven inks. **Do not redo any of that, do not restyle, do
not recompose.** What is left is what the set *says*, and it is the part that makes it read as a real
build rather than a set of mockups.

The measurements below were taken by rendering the current file at 1:1 and walking all 30 artboards.

---

## 1. A template token is printed on screen

**3a Yard**, in the persistent strip: the literal text `{{ clockNote }}` renders where every other
board reads "the yard waits" / "the cart leaves at dusk" / "the yard holds its breath". Replace it
with the real line for a yard at rest.

Then check the file for any other unsubstituted token before you finish.

## 2. The set must be one save file

Measured, by board:

- **day 23 · 412 koku · seven men · Ayame alive** — 3a, 3b, 4a, 4b, 4c, 4d, 4e, 4f, 4g, 5c, 6e, and
  "Yard — named".
- **day 25–26 · 686 koku · five men · Ayame dead** — 5d Aftermath, 6f Tribunal. These two are the
  consequence of the day-23 contract and may stay later, as long as they stay the *same* later
  moment and say so plainly.
- **wrong, and the most visible of all** — **6a Title**'s Continue row ("day 25 of 60 · five men
  standing") and **6c Save slots** slot 1 (day 25). These two screens describe the save the player is
  about to enter, and no screen behind them matches. Move both to **day 23 · seven men · 412 koku**.
- **specimen copy carrying the wrong day** — **2a Plate** ("day 25" in the type specimen and in the
  dominant-act demo) and **7a Overlay anatomy** ("Day 25" on the "whole stage" example, sitting
  beside a "Day 23" example on the same board). Move both to day 23.

Then settle the men. Today the same roster is described four ways: **7** (4f, 6e), **6** (3b, 2b,
2a), **5** (6a), **four in the dojo** (2b). Pick **seven on the roster**, and wherever a smaller
number appears make the copy say what it is counting — men sent out, men fit to fight, men in the
yard tonight — rather than leaving a bare count that contradicts the ledger.

Last one: **5b Fight — pull-out confirm** says "4 medicine, 6 days abed. The chest holds three."
The cost and the store contradict each other in the same sentence. Decide which is true and make the
other follow. On the same board, the dimmed background keeps only Ren and Genba, while 5a stages
Sada as well — restore the third card so the two boards read as one moment.

## 3. The failure state still does not exist

Two boards were added since the last pass — **"Term lost"** (the term ending nineteen days early,
the storehouses empty) and **"A viewer at the gate"** (a viewer from chat waiting to be let in).
Both are good, and neither is the missing piece.

What no artboard in the file shows is **an act the player committed to that did not happen**: the
smith who would not take the work, the man who refused to go, the crowd connection dropping in the
middle of a fight, a contract that expired while the panel was open. Add one artboard for it.

It must say, in the world's language and not as a dialogue box: what was attempted, what went wrong,
what it cost (and what it did not), and what the player may do now. Then add its shape to the
confirm family so the anatomy is complete.

## 4. 7a is not yet the complete anatomy of a decision

The confirm family is spread across boards: plain confirm and the irreversible "Cut" family on 7a,
the costly confirm on 5b and 4c, the write-over on 6c, and the **blocked confirm with the live
counter — "2 of 3 chosen"** on 2a and 3b only.

Bring the counter variant onto **7a** as well, beside the other shapes, and add the failure shape
from item 3. A reader should be able to look at 7a alone and see every form a decision can take.

## 5. 7c Flow does not describe the set it belongs to

- Its headline still reads **"THE WHOLE SET · TWENTY-TWO ARTBOARDS"**. The file holds **30** boards
  with a screen label. Fix the count, and write it so a later board does not make it wrong again.
- **6e Pause** is still not a node — it appears only as the words "Stopped: 6e" inside the yard node.
- **2a, 2b, 7a, 7b, 7c** and the two new boards are still footer prose rather than nodes.
- There is still **no legend and no per-node marker** for *opens over the yard* / *takes the whole
  stage* / *before the yard*. The grouping implies it; nothing states it.

## 6. The persistent strip is still not persistent

Measured, field by field, on the boards that carry it:

- **water is missing** on 4a Armoury, 4d Infirmary, 4e Shrine, 6e Pause, 5c Province
- **sake appears only** on 4e Shrine
- the contextual additions are fine and should stay — "−60 if you buy the sleeve" (4a), "−48 in the
  basket" (4c), "3 medicine · 2 days — Genji needs 6" (4d), "−18 if they go" (3b)

Fix the base: **koku, rice, water, medicine, the summons, the clock line** appear on every board that
shows the strip, in that order, always. A contextual delta hangs off its own field; it never replaces
a field, and no field is ever dropped for space.

## 7. Armour tier fails at unit-card size

At portrait size the four silhouettes on 2a are unambiguous. On the unit cards in **5a** the robe and
the partial lamellar glyphs differ by a faint shoulder bump, and only the horned ō-yoroi reads
reliably. Redraw the card-size glyphs so all four separate at that size — push the one cue each tier
owns (drape, fitted edge, standing plates, horns) rather than adding detail — and put the card-size
row on **2a** beside the portrait row, so the plate proves its own claim at both sizes.

## 8. Deliver

Order: 1, 2, 6 first (they are wrong facts on screen), then 3 and 4, then 5, then 7.

At the end, state: which boards changed, the final day/koku/roster numbers the set now agrees on, and
anything you could not do without restyling — listed, not silently skipped.
