<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Domina.Core\Domina.Core.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <!-- The tool is an application, not a library: the types stay internal and the tests see them. -->
    <InternalsVisibleTo Include="Domina.Sim.Tests" />
  </ItemGroup>

</Project>
